# frontend

Glemail frontend, built with Lustre and TailwindCSS (with custom Shadcn UI components and implementation)

## Running

We use just for simplifying commands

```sh
just install # install deps
just start # starts server
```

<!-- `gleam run -m lustre/dev start` -->
